import React from 'react';
import PropTypes from 'prop-types';

import DatasetListedDetails from 'components/DatasetListedDetails/DatasetListedDetails'

describe('(Component) DatasetListedDetails', () => {
  it('should exist', () => {

  })
})
