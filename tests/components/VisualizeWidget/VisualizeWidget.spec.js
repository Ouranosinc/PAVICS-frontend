import React from 'react';
import PropTypes from 'prop-types';

import VisualizeWidget from 'components/VisualizeWidget/VisualizeWidget'

describe('(Component) VisualizeWidget', () => {
  it('should exist', () => {

  })
})
