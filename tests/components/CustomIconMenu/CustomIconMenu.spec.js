import React from 'react';
import PropTypes from 'prop-types';

import CustomIconMenu from 'components/CustomIconMenu/CustomIconMenu'

describe('(Component) CustomIconMenu', () => {
  it('should exist', () => {

  })
})
