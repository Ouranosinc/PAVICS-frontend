import React from 'react';
import PropTypes from 'prop-types';

import MonitoringJobsList from 'components/MonitoringJobsList/MonitoringJobsList'

describe('(Component) MonitoringJobsList', () => {
  it('should exist', () => {

  })
})
