import React from 'react';
import PropTypes from 'prop-types';

import WidgetDrawFeatures from 'components/WidgetDrawFeatures/WidgetDrawFeatures'

describe('(Component) WidgetDrawFeatures', () => {
  it('should exist', () => {

  })
})
