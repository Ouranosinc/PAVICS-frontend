import {types, actions, reducers} from 'redux/modules/WorkflowAPI'

describe('(Redux) WorkflowAPI', () => {
  describe('(Reducer)', () => {
    it('sets up initial state', () => {
      // expect(reducer(undefined, {})).to.eql(initialState)
    })
  })
})
